// Session 14
// Create a namespace called Session14Activites
// Declare a class that is inside of that namespace that does not inherit anything
// Have a public and private integer value
// Have a method that returns the sum of those values


using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Activities2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
